# Load the definitions file on startup
management.load_definitions = /etc/rabbitmq/definitions.json

# Optional: Ensure the guest user can access from any network
# (Useful for development if not using your 'admin' user)
loopback_users.guest = false